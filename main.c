    #include <stdio.h>
    #include <stdlib.h>
    #define cant 5

    int main()
    {
        int legajo[cant];
        float salario[cant];
        char nombre[cant][31];

        for (i=0;i<cant;i++)
        {
            printf("legajo: ");
            scanf("%d",&legajo[i]):
            printf("salario: ");
            scanf("%f",&salario[i]);
            fgets(nombre[i],)
        }
        return 0;
    }
